# fastapi y starlette van fijas y juntas: el 500 de /aulas/<id> salió de que
# producción construía una FastAPI más nueva que la del entorno local, con otra
# estructura interna de rutas. fastapi declara starlette>=0.46 sin tope, así que
# sin fijar ambas el par vuelve a separarse en el próximo build.
fastapi==0.136.0
starlette==1.0.0
uvicorn[standard]
python-multipart
a2wsgi
Werkzeug==3.0.1
SQLAlchemy>=2.0
psycopg[binary]>=3.1
python-dotenv==1.0.0
reportlab==4.0.7
Pillow==10.1.0
openpyxl==3.1.2
fpdf==1.7.2
numpy==1.26.4
pandas==2.2.0
python-dateutil==2.9.0.post0
pytz==2025.2
jinja2
itsdangerous
httpx
pytest
